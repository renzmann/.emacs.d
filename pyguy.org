Starting a session with =:session= keeps a long-running python for all blocks.

#+BEGIN_SRC python :python ~/.emacs.d/.venv/bin/python :session
  hello = "!"
  hello
#+END_SRC

#+RESULTS:
: !

#+begin_src python :session
  hello
#+end_src

#+RESULTS:
: !

#+begin_src python :session
  hello
#+end_src

#+RESULTS:
: Hello, world!

Below, we try a couple examples of raising an error. They occur in the
running *Python* buffer, instead of appearing below

#+begin_src python :session :results output
  nobody_here
#+end_src

#+RESULTS:

#+begin_src python :session
  raise ValueError("What????")
#+end_src

#+RESULTS:

The fact we /have/ to save to a file and then have the cell return the
same filename is a bit Byzantine. Any way to make a QoL improvement
there?

#+BEGIN_SRC python :session :results file
  import numpy, math, matplotlib.pyplot as plt

  x = numpy.linspace(0, 2*math.pi)
  fig = plt.figure()
  plt.plot(x, numpy.sin(x))
  fig.savefig("img.png")
  "img.png"
#+END_SRC

#+RESULTS:
[[file:img.png]]

#+BEGIN_SRC python :session
  1 + 2
#+END_SRC

#+RESULTS:
: 3

#+begin_src python :session
  import sys
  sys.executable
#+end_src

#+RESULTS:
: /home/renzmann/.emacs.d/.venv/bin/python
